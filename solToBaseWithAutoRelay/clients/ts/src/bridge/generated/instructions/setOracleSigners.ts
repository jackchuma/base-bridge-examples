/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BRIDGE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getBaseOracleConfigDecoder,
  getBaseOracleConfigEncoder,
  type BaseOracleConfig,
  type BaseOracleConfigArgs,
} from '../types';

export const SET_ORACLE_SIGNERS_DISCRIMINATOR = new Uint8Array([
  38, 183, 212, 67, 28, 241, 25, 160,
]);

export function getSetOracleSignersDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_ORACLE_SIGNERS_DISCRIMINATOR
  );
}

export type SetOracleSignersInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountBridge extends string | AccountMeta<string> = string,
  TAccountGuardian extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBridge extends string
        ? WritableAccount<TAccountBridge>
        : TAccountBridge,
      TAccountGuardian extends string
        ? ReadonlySignerAccount<TAccountGuardian> &
            AccountSignerMeta<TAccountGuardian>
        : TAccountGuardian,
      ...TRemainingAccounts,
    ]
  >;

export type SetOracleSignersInstructionData = {
  discriminator: ReadonlyUint8Array;
  cfg: BaseOracleConfig;
};

export type SetOracleSignersInstructionDataArgs = { cfg: BaseOracleConfigArgs };

export function getSetOracleSignersInstructionDataEncoder(): FixedSizeEncoder<SetOracleSignersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['cfg', getBaseOracleConfigEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ORACLE_SIGNERS_DISCRIMINATOR })
  );
}

export function getSetOracleSignersInstructionDataDecoder(): FixedSizeDecoder<SetOracleSignersInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['cfg', getBaseOracleConfigDecoder()],
  ]);
}

export function getSetOracleSignersInstructionDataCodec(): FixedSizeCodec<
  SetOracleSignersInstructionDataArgs,
  SetOracleSignersInstructionData
> {
  return combineCodec(
    getSetOracleSignersInstructionDataEncoder(),
    getSetOracleSignersInstructionDataDecoder()
  );
}

export type SetOracleSignersInput<
  TAccountBridge extends string = string,
  TAccountGuardian extends string = string,
> = {
  /** The bridge account containing configuration */
  bridge: Address<TAccountBridge>;
  /** The guardian account authorized to update configuration */
  guardian: TransactionSigner<TAccountGuardian>;
  cfg: SetOracleSignersInstructionDataArgs['cfg'];
};

export function getSetOracleSignersInstruction<
  TAccountBridge extends string,
  TAccountGuardian extends string,
  TProgramAddress extends Address = typeof BRIDGE_PROGRAM_ADDRESS,
>(
  input: SetOracleSignersInput<TAccountBridge, TAccountGuardian>,
  config?: { programAddress?: TProgramAddress }
): SetOracleSignersInstruction<
  TProgramAddress,
  TAccountBridge,
  TAccountGuardian
> {
  // Program address.
  const programAddress = config?.programAddress ?? BRIDGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bridge: { value: input.bridge ?? null, isWritable: true },
    guardian: { value: input.guardian ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.bridge),
      getAccountMeta(accounts.guardian),
    ],
    programAddress,
    data: getSetOracleSignersInstructionDataEncoder().encode(
      args as SetOracleSignersInstructionDataArgs
    ),
  } as SetOracleSignersInstruction<
    TProgramAddress,
    TAccountBridge,
    TAccountGuardian
  >;

  return instruction;
}

export type ParsedSetOracleSignersInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The bridge account containing configuration */
    bridge: TAccountMetas[0];
    /** The guardian account authorized to update configuration */
    guardian: TAccountMetas[1];
  };
  data: SetOracleSignersInstructionData;
};

export function parseSetOracleSignersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetOracleSignersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bridge: getNextAccount(),
      guardian: getNextAccount(),
    },
    data: getSetOracleSignersInstructionDataDecoder().decode(instruction.data),
  };
}
